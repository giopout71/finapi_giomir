trigger: none

resources:
  pipelines:
    - pipeline: giopout71.finapi_giomir_build
      source: giopout71.finapi_giomir_deploy
      trigger:
        branches:
          include:
            - main

pool:
  name: student-lab-pool

steps:
  - script: |
      /azagent/venv/bin/pip3 install pytest requests
    displayName: "Install dependencies"

  - script: |
      /azagent/venv/bin/python3 -m pytest -v --tb=line  -W "ignore::DeprecationWarning" -s tests/smoke_test.py --junitxml=smoke-results.xml
    displayName: "Run full smoke test suite"


  - task: PublishTestResults@2
    condition: always()
    inputs:
      testResultsFiles: 'smoke-results.xml'
      testRunTitle: 'Post-deployment Smoke Test'
      failTaskOnFailedTests: false